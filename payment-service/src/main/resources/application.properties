spring.application.name=PAYMENT-SERVICE
server.port=8087

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/micro_payment_db
spring.datasource.username=postgres
spring.datasource.password=akshay

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Eureka Configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=true

# Logging
logging.level.com.micro.payment_service=DEBUG

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# ============================================
# RAZORPAY CONFIGURATION (ADD THIS)
# ============================================
# Get these keys from https://dashboard.razorpay.com/
# For testing, use test keys (rzp_test_xxx)
razorpay.key.id=rzp_test_T0GLB9qcWafTo4
razorpay.key.secret=VJA0Yfsy3hYGfnaSekYdA0VZ